import React, { FunctionComponent } from 'react';
import type { StringIndexedObject } from '~/types';

export interface RowsProps {
  rows: StringIndexedObject[];
}

const Rows: FunctionComponent<RowsProps> = function ({}) {
  return <></>;
};

export default Rows;
